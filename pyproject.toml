[project]
name = "eink-image-toolkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numba>=0.65.1",
    "numpy>=2.4.3",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
